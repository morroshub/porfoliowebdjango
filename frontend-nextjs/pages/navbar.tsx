import React from 'react';
import styled from 'styled-components';

const StyledNavbar = styled.nav`
    /* Estilos de tu navbar */
`;


const Navbar = () => {
    return (
        <nav>
            {/* Contenido de tu navbar */}
        </nav>
    );
};

export default Navbar;